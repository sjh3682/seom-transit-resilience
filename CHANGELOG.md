# 변경 이력

## v7.0 — v6 대비 변경점

| 영역 | v6 | v7 |
|---|---|---|
| 왕복 노선 | 서로를 대체 노선으로 셈 (섬 항로 영향 0) | 운행계통으로 통합 |
| 점수 | 지도(Python 가중합) ≠ 버튼(JS 별도 공식) | 같은 엔진이 계산해 항상 일치 |
| TVS 요소 | 기획서 5요소 중 3개 없음 | 5요소 모두 제거 전후 변화로 계산 |
| 정류장 폐쇄 | 노선이 그 자리에서 끊김 | 통과 + 대체 정류장까지 도보 |
| 경로찾기 | 방향 무시 | 정차 순서 방향 준수, 경로 회복력(핵심 노선이 끊기면 +몇 분) |
| AI | Isolation Forest (순환 논리) | SHAP 설명 모델 + 취약 유형 군집 + 대리 모델 경로 |
| 지역 지도 | 정류장 없는 격자는 빈칸 | 서비스 공백 칸으로 표시, 시군구 집계 |
| 렌더링 | 줌 8.3에서 1프레임 0.8초 이상 | 모든 레이어·줌에서 37ms 이하, 조작 중에는 비트맵만 이동 |
| 로딩 | 메인 스레드 JSON.parse (최대 3.5초 멈춤) | 워커에서 이진 데이터 해제 후 transfer (약 2초, 멈춤 없음) |
| 파일 크기 | 17.9MB | 14.7MB |
| 화면 | 760~1120px에서 패널 못 닫음, 선택 해제 없음, 배경 4.5% 흐림 | 패널 접기·닫기, 해제 경로 3가지, 흐림 완화, 호버 툴팁, 겹친 노선 선택 목록, 뒤로가기 |
| 버그 | "\27" 표시, 고령 버튼 무동작, 검색 순서, 터치 불가 등 | 모두 수정 |
| 재현성 | v5 HTML에서 추출, 경로 하드코딩 | GTFS부터 재현 가능, 설정 파일 하나, 테스트 포함 |

---

### v7.1 수정 (사용 중 발견)

| 문제 | 원인 | 수정 |
|---|---|---|
| 정류장의 19.7%가 "매우 취약", 2.5만 개가 82.35점 동점 | 대체 정류장을 400m 안에서만 찾아 그 밖은 모두 "완전 고립" 처리 | 800m까지 찾고 도보 부담은 실제 거리로. 등급 비율이 50·30·15·5%로 돌아옴(테스트 추가) |
| 정류장 AI 설명 모델 R² 0.93 | 위의 절벽(400m 안 정류장 0개 → 82점)을 학습 | 수정 후 0.76 — 더 정직한 값 |
| 배경지도에 "API KEY REQUIRED" | CARTO가 키 없는 요청에 워터마크 | 무료 키를 설정 파일로 연결, 키가 없으면 경계선 배경 |
| 탭을 바꿔도 이전 선택이 남아 지도가 비어 보임 | 선택과 레이어 탭이 따로 동작 | 선택하면 그 레이어로 전환, 다른 탭으로 가면 선택 해제 |
| 1024px·모바일에서 선택 노선이 패널 뒤로 숨음 | 패널을 열기 전에 지도 맞춤을 계산 | 패널을 먼저 열고 실제 패널 폭·높이로 맞춤 |
| 태블릿 첫 화면에서 패널이 지도를 덮음 | 반응형 코드의 죽은 분기 | 서랍 폭에서는 선택이 없을 때 닫힘 |
| 전국 노선 지도가 온통 빨강·검정, 점선이 실선처럼 보임 | 낮은 등급을 숨김, 둥근 선 끝이 틈을 메움 | 낮은 등급은 옅은 회색 바탕선, 점선·파선은 각진 끝 |
| AI 설명 "도보권 정류장 수 +47점"이 헷갈림 | 값 없이 항목 이름만 표시 | "573m (전국 중앙값 172m) · 위험 +12.7점"처럼 실제 값과 중앙값 표시 |
| 경로찾기가 왼쪽 패널 맨 아래 | 배치 | 맨 위로 |

### v7.2 수정 (화면 전수 점검)

| 문제 | 수정 |
|---|---|
| 도시철도와 섬 항로가 같은 검정이라 헷갈림 | 노선마다 위험 성격 표시: **끊기면 고립**(단절·대체 없음) / **끊기면 지연**(우회 가능하나 크게 느려짐) |
| "서울1호선 · 충남 천안시 서북구" | 여러 시도를 지나는 노선은 "충남·경기·서울"처럼 표기 |
| AI 설명·고령 비율 칸 줄바꿈 깨짐 | 두 줄 배치, 값과 설명 분리 |
| 5km 격자를 눌러도 확대 안 되고 패널이 거의 빈칸 | 칸으로 확대, 격자 안 정류장 목록·빈 격자는 가장 가까운 정류장 거리 |
| 제거 시뮬레이션의 주황 곡선이 지저분함 | 영향 받은 구간만 노선을 따라 빨강(단절)·주황(지연) 띠 + 지도 범례 |
| 정류장 전국 지도가 온통 검정 | 전국 축척은 매우 취약만 작은 점으로, 나머지는 회색 바탕점 |
| 검색 결과가 없을 때 무반응 | 안내 문구 |
| 태블릿에서 노선 선택 시 지도가 거의 안 보임 | 서랍이 열리면 왼쪽 패널 자동 접기(선택 해제 시 복원) |
| 정책 결과 표시 중 오류(점검 중 발견) | 정책 적용 후 결과도 같은 경로 계산 사용 |

### v7.3 수정

| 문제 | 원인 | 수정 |
|---|---|---|
| 시군구 경계가 지도와 어긋남 | 2013년 단순화 경계(전국 꼭짓점 8,444개)를 소수 셋째 자리(약 100m)로 한 번 더 뭉갬 | 2025년 7월 행정동 경계를 시군구·시도로 합쳐 위상 보존 40m 단순화(꼭짓점 약 12만 개), 좌표는 넷째 자리. 섬·해안선이 실제 모양대로 그려지고, 경계 밖 정류장이 1,871개에서 143개로 줄어 섬 지역 오표기(국화도→당진)가 사라짐 |
| 정밀 경계로 바꾸자 지역 지도 그리기 405ms | 섬이 많아 고리 수천 개 × closePath, 전국 축척에서도 모든 꼭짓점 그리기 | closePath 대신 시작점으로 잇기 + 0.7px 이내 꼭짓점 생략 → 5ms |
| 브이월드가 떠야 할 때도 CARTO로 넘어갈 수 있음 | 실패 응답(한반도 밖 타일)이 성공보다 먼저 와서 "실패 8개"로 오판 | 한반도 범위 밖 타일은 요청하지 않고, 요청한 타일이 모두 도착했는데 성공이 0개일 때만 전환 |

`tests/ui_audit.py` 가 모든 레이어를 전국·중간·도시 축척에서 그려 **1회 그리기 60ms 초과를 문제로 잡는다**(렉 재발 방지).

### v7.4 수정 (버벅거림)

사용자 녹화 영상에서 지도를 끌 때 0.3~0.5초마다 뚝뚝 끊기는 현상을 **150% 배율 화면 + CPU 4배 느리게** 조건으로 재현해 원인을 하나씩 측정했다.

| 원인 | 측정 | 수정 |
|---|---|---|
| 드래그 중에도 0.45초마다 지도를 다시 그림 | 다시 그릴 때마다 2.4초 멈춤이 뒤따름 | 드래그·핀치 중엔 그림을 옮기기만 하고 손을 뗀 뒤 한 번만 그림 → 드래그 중 끊김 0회 |
| 전국 노선 지도의 회색 바탕선(안정·주의 1만 4천여 개) | 이것만 끄면 2.3초 → 0.4초 | 제거(전국 축척은 취약 이상만, 확대하면 전체) |
| 150% 배율에서 칠할 픽셀 2.25배 | 배율 1이면 2.3초 → 0.45초 | 데이터 층만 해상도 1.25배로 제한(선택 강조는 선명하게, 지도 저장은 2배 해상도) |
| "주의" 노선까지 파선 | 도심(노선 774개)에서 265ms → 164ms | 주의는 실선, 취약·매우 취약만 파선·점선 |

기존 속도 점검은 자바스크립트 시간만 재서 **선을 픽셀로 칠하는 시간을 놓쳤다**. `tests/ui_audit.py` 는 이제 150% 배율에서 "화면에 보일 때까지"(300ms 초과 = 문제)와 CPU 4배 느린 조건의 드래그 중 프레임(100ms 초과 = 문제)을 잰다.

### v7.5 수정 (원본 GTFS · 정책 조합 · 항로 · 2026 행정구역 · 비용)

| 항목 | 이전 | 지금 |
|---|---|---|
| 소요시간 | 거리·속도 추정(v6 HTML에서 복원) | **KTDB 대중교통 GTFS(2025.3) 실제 도착·출발 시각**. route_type 은 KTDB 자체 코드(0 시내버스 … 7 항공)라 설명서대로 해석(국제 표준으로 읽으면 버스가 트램이 됨) |
| 불러오기 속도 | 운행마다 열 전체 복사(O(n²)) | 조각마다 한 번 → 30만 행 144초 → 2.4초 |
| 같은 노선의 변형 패턴 | 이름·포함 관계로만 통합 | 같은 노선 ID 는 한 운행계통(31,149 패턴 → 17,574 계통) |
| 항로 영향 인구 | 항구 주변 인구(백령도 항로 210명) | 해운을 뺀 교통망에서 본토와 이어지지 않는 섬(295곳·7만 7,588명)의 인구를 항로 수로 나눔(백령도 항로 2,770명) |
| 행정구역 | 2025년 경계·이름 | 행정동을 2026 행정구역으로 다시 묶은 경계: 전남광주통합특별시, 인천 제물포구·영종구·서해구·검단구 |
| 비용 | 버스 대·년, DRT 비용비 0.5 가정 | 억 원/년: 시내버스 788,484원/대·일(광주 2024 표준운송원가), DRT 558,380원/대·일(국토부 DRT 가이드라인, 이천 똑버스) |

### v7.6 수정 (전체화면 노선·구간 렉)

원본 GTFS로 데이터가 늘어난 뒤, 전체화면에서 노선·구간 레이어가 버벅였다. 150% 배율·전체화면 크기로 재현해 원인을 하나씩 쟀다(점선·반투명·선 굵기는 영향이 거의 없었다).

| 원인 | 수정 | 효과(정상 속도, 수도권 줌 9.5~11) |
|---|---|---|
| 한 도로에 노선 수백 개가 겹쳐, 겹친 윤곽을 모두 칠함 | 같은 정류장 쌍 구간은 가장 높은 등급으로 **한 번만** 긋기(겹친 곳은 원래도 맨 위 색만 보임) | 노선 약 1.5초 → 0.5초 |
| 전국 축척 구간의 83%가 1픽셀보다 짧음 | 1.5px 미만 구간은 2px 칸마다 등급별 **점 하나**로 | 전국 구간 약 1.1초 → 0.26초(CPU 4배 느린 조건) |
| 칠하는 시간이 캔버스 픽셀 수에 비례 | 지도 칸이 크면 약 120만 픽셀 예산 안으로 해상도 조정(지도 저장은 2배 고해상도 그대로) | 추가로 약 20~40% |
| 광역 축척에서 안정 노선까지 전부 그림 | 축척별 간소화: 줌 8 미만 취약 이상, 9.5 미만 주의 이상 | 그릴 노선 수 ↓ |

`tests/ui_audit.py` 가 **전체화면 수도권 중간 축척**(가장 무거운 화면)을 재서 700ms 초과를 문제로 잡는다(이번에 한적한 지역·도심 좁은 화면만 재서 놓쳤다).

### v7.7 화면 정리
- 배경 지도 선택 칸 삭제(자동 전환만).
- 지도에서 선택한 대상을 다시 누르면 선택 해제(툴팁에 안내).
- 고령 강조 기준 20% → **40%**(`config/params.json` 의 `display.senior_highlight_pct`). 시군구 고령 비율 중앙값이 30.6%라 20·30%로는 절반 이상이 강조됐다. 40%면 256곳 중 83곳이며, 그중 42곳이 교통 취약 이상이다.
- "대안 비교" → "이 노선이 끊기면? — 대책 비교" / "이 정류장이 멈추면? — 대책 비교".
- 시군구 범례에 "색 없는 곳: 호수·간척지 등 통계 경계 밖(거주 인구 없음)". 시화호·화옹간척지 등은 원본 행정동(통계) 경계에 없다.

### v7.8 결과물 평가 후 정리
- **화면**: 처음 화면을 결론 숫자 6개로(데이터 규모는 신뢰도 칸 한 줄로) · 결과 카드 = 한 문장 요약 → 요인 → 대책 → 상세 표(접기), 결과로 자동 스크롤 · 왼쪽 패널은 레이어 선택이 맨 위, 경로 회복력·표시 설정은 접기 · 대표 사례는 섬 항로 → 농어촌 순 · 쉬운 용어(취약도, 갈 수 있는 병원·역 감소, 끊기는 이동, 도로 구간 …)와 취약도 도움말(?) · 매우 취약(검정) 지역을 진하게 · CSV 내려받기(전국 매우 취약 / 시군구별) · "분석 방법" 창을 현재 결과로 새로 씀.
- **계산 수정 ① 대기시간(중요)**: 원본 GTFS 는 한 노선이 시작역·구간 운행·여러 바퀴 운행으로 수십 개 패턴으로 쪼개져 있는데(서울 2호선 49개, 패턴마다 하루 1~3회), 대기시간을 패턴 하나의 운행횟수로 계산해 상한(30분)까지 부풀었다. 경로 탐색이 도시철도를 거의 타지 않아 2호선을 지워도 변화가 없었다. 같은 노선·같은 방향 구간을 지나는 운행을 모두 합쳐(여러 바퀴는 지나는 횟수만큼) 계산하도록 고쳤다 — 도시철도 대기 중앙값 30분 → 3.3분(2호선 2.1분), 전체 패턴의 15.9% 변경. 회귀 테스트 추가.
- **계산 수정 ② 표본**: 여러 바퀴가 한 운행으로 기록된 순환선은 0·25·50·75·100% 지점이 모두 같은 역이라 표본 이동이 0개였다. 표본이 0개일 때 첫 바퀴에서 다시 뽑는다. 정책 계산 재사용 조건에 엔진 코드 지문을 넣었다.
- **코드**: 긴 함수 분리(p04 main 261 → 85줄, p08 217 → 111줄, p02, 웹 bindUI) — AST 로 구간별 입력·출력을 계산해 옮기고, 정리 전후 graph·scores·policy 결과 지문이 같음을 확인 · 안 쓰는 import·변수·함수 삭제 · v6 HTML 불러오기를 `legacy_v6.py` 로 분리 · 빌드(p06)가 JS 문법을 먼저 검사(한 파일 오류로 페이지 전체가 안 뜨는 사고 방지) · README 는 결과·사용법 중심, 수정 이력은 이 파일로.

### v7.9 가정값 → 계산값
- **비용**: DRT 차량 = 끊긴 노선의 하루 운행횟수 × 모든 고립 정류장을 도는 경로의 주행시간 ÷ 하루 운행시간, 연장 = 연장 노선이 고립 정류장을 나눠 들르는 추가 주행시간. 증편·연장·DRT 모두 운행시간 비례(DRT 만 1대 단위로 올려 불리하던 문제 해결). 휘발유 순발열량 30.4 → 30.1 MJ/L(2022 개정 법령).
- **탄소**: 연비·1인 대중교통 통행수·승용차 전환율 가정을 없애고 공공자료로 계산 — KTDB 2025 여객 O/D 예비조사, 시도별 자동차 등록대수(전남광주 차감 계산), 한국에너지공단 표시연비(국산 휘발유 승용 1,012개 모델 CO₂ 평균 166 g/km), 노선 표본 이동 거리. 연 100억 원 포트폴리오의 CO₂ 범위가 2.3만~20.6만 t → 7.3만~8.5만 t 로 좁혀짐.
- **화면**: 반화면(오른쪽 패널이 지도 위 서랍)에서 접기 손잡이가 패널에 가려지던 문제 → 열린 패널 왼쪽 가장자리에 붙음. 경로 회복력은 맨 위·펼침으로 복구.

### v7.10 노선을 실제 도로 모양으로
- **10단계 `p10_road_network.py`**: 국가 표준노드링크(링크 155만·노드 118만)로 도로망을 만들고, 정류장을 가장 가까운 도로 선 위 지점에 붙인다(94.6%, 거리 중앙값 6m). 교차점에 붙이면 시골 도로에서 되돌아가는 모양이 돼서 선 위 지점을 쓴다.
- **11단계 `p11_road_shapes.py`**: 버스·시외·공항버스 정류장 쌍 18.7만 개를 도로 최단 경로로 잇는다 — 도로 91.0%, 직선 유지 9.0%(정류장이 도로 밖 6.0%, 길 없음 2.7%, 지나친 우회 0.3%). 5m 단순화 후 좌표 131만 점. 출발 정류장 2천 곳마다 저장하고 이어서 계산한다(약 7분).
- **웹**: 도로 선형을 백만분의 1도 정수·차분으로 담는다(웹 데이터 압축 14.0 → 19.8MB, HTML 26.6MB). 줌 11.5 이상에서 노선·구간·선택 강조·끊어보기 결과·경로 회복력 결과를 도로를 따라 그리고, 클릭 판정도 도로 선을 기준으로 한다(직선에서 30px 떨어진 도로 위를 눌러도 잡힘). 전국·광역 축척은 직선 그대로(렉 방지). 공용 함수 `edgePoints`·`traceStops`·`distanceToStops` 로 한 곳에서 처리.

### v7.11 민간데이터 검증 · 심사 관점 보완
- **정류장 '영향 규모' 지표 수정(중요)**: 도보권 인구만 쓰던 것을 `√인구 × 하루 운행횟수` 로 바꿨다. 서울시 버스 교통카드 승하차 실측(2026.8, 7,362곳)과 비교하니 인구만: 순위상관 **0.102**, 바꾼 지표: **0.653**. 도시는 인구가 고르게 분포해 변별력이 없고 실제 이용은 서비스 수준이 좌우한다. `p12_validate_demand.py` 가 후보 지표들을 함께 재서 근거를 남긴다.
- **탄소**: 대중교통현황조사(2024) 시도별 1주 이용횟수 분포로 시도별 이용 빈도 보정(서울 1.026, 강원 0.964 등).
- **화면**: 첫 화면에 CO₂ 감축 추가 · AI 정책 목록에 "섬·농어촌 노선 119곳(21%)에 배정, 19.9만 명 보호" 요약과 '섬·농어촌만' 필터 · 예산을 "DRT 약 N대 1년 운행" 규모로 환산해 표기 · 발표 모드를 전국 지도 → 취약 시군구 → 농어촌 노선 → DRT → 섬 항로 → AI 정책 6단계로 재구성(첫 장면이 항로 직선 하나였던 문제).

### v7.12 제출 점검
- README 5-6장에 v7.4 시절 옛 수치(전수 71분·25% 계산·검증 351개·포착률 100%/무작위 34%·R² 0.947·예산 100억 70개 노선 286,928명·CO₂ 32,305t)가 남아 1·6장 최신 표와 어긋나던 것을 현재 결과로 교체. 5-3 '영향 규모' 정의를 √인구 × 운행횟수로, 5-7에 민간 교통카드·탄소 입력·표준노드링크 자료를 추가. 테스트 개수(엔진 9·데이터 7·파이프라인 6), 단계 수(1~12), GitHub Pages 실제 주소 반영.
